---
build_inventory:
  guests:
    vm100_pfsense:
      enabled: true
      notes: "Stage 1: pfSense only. All other guests are created in the production stage."
    vm050_mint:
      enabled: false
      notes: "Created in the production stage, not here."
    vm210_ai_gpu:
      enabled: false
    lxc066_docker_arr:
      enabled: false
    lxc200_docker_services:
      enabled: false
    lxc220_docker_apps:
      enabled: false
    lxc230_docker_media:
      enabled: false
    lxc240_docker_external:
      enabled: false
    lxc250_infra:
      enabled: false
