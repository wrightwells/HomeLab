services:
  influxdb:
    image: influxdb:2
    container_name: influxdb
    restart: unless-stopped
    environment:
      - TZ=Europe/London
    ports:
      - "8086:8086"
    volumes:
      - ./data:/var/lib/influxdb2
