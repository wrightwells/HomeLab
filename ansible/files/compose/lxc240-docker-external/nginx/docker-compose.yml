services:
  nginx:
    image: nginx:1.28-alpine
    container_name: nginx
    restart: unless-stopped
    env_file:
      - stack.env
    environment:
      TZ: ${TZ}
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./files/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./files/default.conf:/etc/nginx/conf.d/default.conf:ro
      - ./files/default.conf:/etc/nginx/conf.d/ssl.conf:ro
      - /mnt/appdata/docker_volumes/nginx/html:/usr/share/nginx/html:ro
      - /mnt/appdata/docker_volumes/nginx/logs:/var/log/nginx
    healthcheck:
      test: ["CMD-SHELL", "nginx -t || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
