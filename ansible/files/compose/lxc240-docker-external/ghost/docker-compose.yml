services:
  ghost:
    image: ghost:latest
    container_name: ghost
    restart: unless-stopped
    environment:
      - TZ=Europe/London
    ports:
      - "2368:2368"
    volumes:
      - /mnt/appdata/docker_volumes/ghost:/var/lib/ghost/content
