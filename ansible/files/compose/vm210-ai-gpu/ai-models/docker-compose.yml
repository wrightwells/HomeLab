services:
  ollama:
    image: ollama/ollama:latest
    container_name: ollama
    restart: unless-stopped
    environment:
      - TZ=Europe/London
    ports:
      - "11434:11434"
    volumes:
      - ./data:/root/.ollama
