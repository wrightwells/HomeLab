---
ansible_ssh_private_key_file: "{{ lookup('env', 'HOME') + '/.ssh/id_ed25519' }}"
ansible_ssh_common_args: "-o IdentitiesOnly=yes"
# Override docker_hosts password-based auth -- VMs use key-based auth only.
ansible_ssh_pass: ""
